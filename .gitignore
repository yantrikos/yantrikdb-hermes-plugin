__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
venv*/   # stray probe venvs (venv113x etc) must never be committed
env/
*.egg-info/
dist/
build/
.coverage
htmlcov/
.DS_Store
*.log
.env
hermes-test/data/

# Local MCP server config — contains host/URL details, not for a public repo
.mcp.json
# Local scratch design docs and screenshots
DEEP-RESEARCH-V05-DESIGN.md
hermes-test/scripts/*.png

# ── Local config that carries credentials ────────────────────────────
# Kept in step across the YantrikDB repos after a `git add -A` swept a
# live API key out of `.mcp.json` into a commit on 2026-08-26; only
# GitHub push protection stopped it being published.
.mcp.json
.mcp.local.json
.env
.env.*
!.env.example
.claude/
*.local.json
